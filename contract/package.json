{
  "name": "contract",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "solcjs --abi --bin -o ./build ./contracts/*.sol",
    "deploy": "node deploy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ethersproject/abi": "^5.7.0",
    "@hashgraph/sdk": "^2.26.0",
    "dotenv": "^16.3.1",
    "solc": "^0.8.20"
  }
}
